# Verified with Python 3.9 on macOS (Apple M2, MPS) and Kaggle T4 x2.
# Version floors are loose; the pins below are what the reported runs used.
torch>=2.0
torchvision>=0.15
numpy>=1.24,<2          # torch 2.2 wheels are built against the NumPy 1.x ABI
pillow>=10.0            # JPEG round trip and resampling filters
pandas>=2.0             # parquet -> arrays
pyarrow>=14.0
scikit-learn>=1.3       # roc_auc_score
requests>=2.31          # full-resolution slice download
