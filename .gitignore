__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
.claude/
.DS_Store

# Datasets are regenerated by `python -m src.get_data` and
# `python -m src.fetch_fullres` (527MB combined).
data/

# Intermediate score dump, regenerated by eval_robustness --save-scores.
results/*.npz
results/*.csv

# The two trained checkpoints ARE committed (6MB each): src/predict.py is a
# required deliverable and must run from a clean clone without retraining.
*.pth
*.ckpt
